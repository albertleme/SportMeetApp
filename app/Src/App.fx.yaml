App As appinfo:
    BackEnabled: =true
    OnStart: =ClearCollect(colMenuItems,tbMenuItens)
    StartScreen: =scr_Login
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

